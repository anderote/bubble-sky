# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache.
org.gradle.configuration-cache=false

# Fabric Properties (pinned for bubble-sky — MUST match bubble-sky-mod)
minecraft_version=1.21.6
yarn_mappings=1.21.6+build.1
loader_version=0.19.3
loom_version=1.11.8

# Mod Properties
mod_version=1.0.0
maven_group=net.bubblesky
archives_base_name=towerdefense

# Dependencies
fabric_api_version=0.128.2+1.21.6
